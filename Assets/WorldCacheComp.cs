using UnityEngine;

public class WorldCacheComp : MonoBehaviour
{
    [SerializeField]
    private int m_totalTiles;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void AddTileToCache(Transform t)
    {

    }
}
